# API (Section 5+)
API_BASE_URL=http://localhost:3001
WS_BASE_URL=ws://localhost:3001

# Firebase (Section 4+ — add google-services.json / GoogleService-Info.plist locally)
# FCM is configured via platform files, not env vars on mobile
